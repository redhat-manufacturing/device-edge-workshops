# Generated by the workshop provisioner
[aws]
hypervisor ansible_host={{ hypervisor_node_facts.instances[0].public_ip_address }} ansible_user={{ hypervisor_node_facts.instances[0].tags.username }}
[aws:vars]
{% if ssh_port is defined %}
ansible_port={{ ssh_port }}
{% endif %}
ansible_ssh_private_key_file="{{ workshop_folder }}/ssh-key.pem"

[hypervisors]
hypervisor ansible_host={{ hypervisor_node_facts.instances[0].public_ip_address }} ansible_user={{ hypervisor_node_facts.instances[0].tags.username }}
