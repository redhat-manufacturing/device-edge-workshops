---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cert-manager
  namespace: openshift-gitops
  labels:
    application: acp-standard-services
spec:
  destination:
    name: ""
    server: https://kubernetes.default.svc
  project: default
  source:
    repoURL: https://github.com/redhat-manufacturing/device-edge-workshops.git
    targetRevision: {{ .Values.gitBranch | default "main" }}
    path: provisioner/charts/cert-manager/
    helm:
      values: |
        baseZone: {{ .Values.baseZone }}
        clusterName: {{ .Values.clusterName }}
{{- with .Values.zeroSSL }}
        zeroSSL:
          email: {{ .email }}
          keyID: {{ .keyID }}
          key: {{ .key }}
{{- end }}
{{- with .Values.cloudflare }}
        cloudflare:
          email: {{ .email }}
          token: {{ .token }}
{{- end }}