{{- $uniqueDataSources := include "deduplicateDataSources" .Values.exampleVirtualMachines | fromYaml }}
{{- range $uniqueDataSources }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: wait-for-datasource-{{ .dataSource }}
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  activeDeadlineSeconds: 300
  template:
    spec:
      restartPolicy: Never
      serviceAccountName: datasource-checker
      containers:
      - name: wait-for-datasource-{{ .dataSource }}
        image: registry.redhat.io/openshift4/ose-cli:latest
        command:
        - /bin/sh
        - -c
        - |
          while true; do
            oc get datasource {{ .dataSource }} -n openshift-virtualization-os-images && break
            echo "Waiting for datasource '{{ .dataSource }}'..."
            sleep 5
          done
  backoffLimit: 10
{{- end }}